{
  "name": "watchlyst",
  "version": "1.0.1",
  "description": "A bot which lets a Discord's server staff to create a list of troublesome users which will notify them once they join the server.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Araraura",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.16.1",
    "discord.js": "^12.4.1",
    "pg": "^8.5.1"
  }
}
